# Project Log

[2026-04-21] | RAG Ingestion Pipeline | Created `src/rag/ingest_data.py` to ingest, chunk, embed, and store PDF and TXT data into ChromaDB | Implementing multimodal RAG data ingestion pipeline according to INSTRUCTIONS_FOR_AGENT.md specifications.
[2026-04-21] | RAG Ingestion Pipeline Fixes | Updated `src/rag/ingest_data.py` | Added `autodetect_encoding=True` to TextLoader and improved vector store persistence by processing in batches and filtering empty chunks to fix embedding runtime errors.
[2026-04-21] | RAG Architecture Update | Updated `src/rag/ingest_data.py` | Migrated to `HuggingFaceEmbeddings` (sentence-transformers/all-MiniLM-L6-v2) to resolve Google API 404 error, refactored Chroma import to use `langchain_chroma`, and reverted TextLoader kwargs.
[2026-04-21] | Phase 2 — Gradio Frontend & RAG Chain | Created `src/ui/app.py` | Built the complete Multimodal RAG application: (1) loads persisted ChromaDB from `data/chroma_db_gigi` via `langchain_chroma` + `HuggingFaceEmbeddings`; (2) initialises `ChatGoogleGenerativeAI` (Gemini 1.5 Flash) reading GOOGLE_API_KEY from `.env`; (3) implements `process_query(message, history)` that retrieves top-4 relevant dental context chunks, encodes any uploaded image as a base64 data URI, and sends a structured multimodal prompt to Gemini; (4) wraps everything in a dark-themed `gr.ChatInterface` (multimodal=True) with example queries and a medical disclaimer; (5) updated `requirements.txt` with `langchain-chroma`, `sentence-transformers`, `langchain-text-splitters`, and `langchain-core`.
[2026-04-21] | Gradio 6.0 Compatibility Fix | Updated `src/ui/app.py` | Fixed 3 issues exposed at runtime: (1) removed deprecated `retry_btn`, `undo_btn`, `clear_btn` kwargs from `gr.ChatInterface` (removed in Gradio 6.0); (2) moved `theme` and `css` arguments from `gr.Blocks()` constructor to the `app.launch()` call per Gradio 6.0 API change; (3) migrated `HuggingFaceEmbeddings` import from deprecated `langchain_community.embeddings` to `langchain_huggingface`.
[2026-04-21] | Runtime Bug Fix — LLM Model & Gradio Warning | Updated `src/ui/app.py` | (1) Fixed `ClientError 404 NOT_FOUND` by changing LLM model name from `"gemini-1.5-flash"` to `"gemini-1.5-flash-latest"` so the v1beta API endpoint resolves correctly; (2) removed `submit_btn` from `gr.ChatInterface` — Gradio 6.0 silently ignores it (and emits a UserWarning) when a custom `MultimodalTextbox` component is provided.

